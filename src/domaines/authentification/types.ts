/* LES FORMES DU DOMAINE AUTHENTIFICATION. */

/* Venues de « Auth.tsx », qui les declarait sans les exporter :
 * un type inerte n a pas a vivre dans le fichier qui le rend. */
export type Mode = "connexion" | "inscription";
